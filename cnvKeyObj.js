'use strict';

// moveTo(x,y) <- where to put the pen down
// lineTo(x,y) <- coords to which the line is draw next


var CnvsKey = (function(){
  cantxt.fillStyle = 'gray';
  cantxt.beginPath();
  cantxt.moveTo(,);
  //draw egg
  cantxt.closePath();
  cantxt.fill();
  cantxt.lineWidth = 4;
  cantxt.strokeStyle = '#00ff00';
  cantxt.stroke();
});
